---
description: Prepare, preview, implement, and verify focused work
---

Read `dev/agent_rules/agent_startup.md` first. Then read `dev/foundation/core/workflows/commands/implement.md` and follow it exactly.

For Tickstrike, write every user-facing phase in Traditional Chinese unless the user explicitly requests another language. Keep code identifiers, file paths, and commands exact.

$ARGUMENTS
