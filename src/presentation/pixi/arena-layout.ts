import arenaDecorationManifest from "./assets/terrain/arena-decoration.json" with { type: "json" };

export const COMPOSITION_WIDTH = arenaDecorationManifest.composition.width;
export const COMPOSITION_HEIGHT = arenaDecorationManifest.composition.height;
export const BOARD_ORIGIN = arenaDecorationManifest.board_origin;
